// Rules

function knoledgeBase(mass, charge, spin, family) {
	var particle = {"name": "", "charge": "", "spin": "", "family": ""}
	
	if ( mass == -1.3e-10 and charge == null and spin == null and family == null ) {
		particle = particles_db[0];
	}
	if ( mass ==  ) {
	}

}
